"use strict"
function solveEquation(a, b, c) {
  let arr = [];
  let discriminant = b**-4*a*c;

  let arr = [discriminant];
  
if(discriminant<0){
  arr=[];
}
if(discriminant===0){
  return arr;
}
}
solveEquation(1,5,4);
function calculateTotalMortgage(percent, contribution, amount, countMonths) {
  
}